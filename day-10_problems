#455_ASSIGN_COOKIES
  
  class Solution(object):
    def findContentChildren(self, g, s):
        g.sort()
        s.sort()
        l=0
        r=0
        while(l<len(g) and r<len(s)):
            if(s[r]>=g[l]):
                l+=1
            r+=1
        return l
